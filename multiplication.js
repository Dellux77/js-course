// Function to print multiplication table
function printTable(num) {
    for (let i = 1; i <= 10; i++) {
        console.log(num + " x " + i + " = " + (num * i));
    }
}

// Example usage:
printTable(5);
